import "./chunk-PR4QN5HX.js";

// node_modules/@convex-dev/auth/dist/providers/Email.js
function Email(config) {
  return {
    id: "email",
    type: "email",
    name: "Email",
    from: "Auth.js <no-reply@authjs.dev>",
    maxAge: 60 * 60,
    // 1 hour
    authorize: async (params, account) => {
      if (typeof params.email !== "string") {
        throw new Error("Token verification requires an `email` in params of `signIn`.");
      }
      if (account.providerAccountId !== params.email) {
        throw new Error("Short verification code requires a matching `email` in params of `signIn`.");
      }
    },
    sendVerificationRequest: config.sendVerificationRequest,
    options: config
  };
}
export {
  Email
};
//# sourceMappingURL=@convex-dev_auth_providers_Email.js.map
